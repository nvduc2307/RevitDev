<Window x:Class="RevitDevelop.Tools.ScreenShot.views.RegionCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RevitDevelop.Tools.ScreenShot.views"
        mc:Ignorable="d"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="#01FFFFFF"
        Topmost="True" ShowInTaskbar="False"
        KeyDown="Window_KeyDown"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        Title="RegionCaptureWindow" 
        Height="450" 
        Width="800">
    <Canvas x:Name="Root">
        <!-- lớp mờ -->
        <Rectangle x:Name="Dim" Fill="Transparent" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}}"
                   Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}}"/>
        <!-- khung chọn -->
        <Rectangle x:Name="Sel" Stroke="#000" StrokeThickness="1" Fill="#1000" Visibility="Collapsed"/>
    </Canvas>
</Window>
