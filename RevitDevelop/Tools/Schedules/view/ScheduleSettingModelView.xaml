<Window x:Class="RevitDevelop.Tools.Schedules.view.ScheduleSettingModelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:RevitDevelop.Tools.Schedules.view"
    mc:Ignorable="d"
    Title="ScheduleView" 
    WindowStartupLocation="CenterScreen"
    MinHeight="400"
    Height="400" 
    Width="500">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>
            <TextBox 
                IsReadOnly="True"
                Text="{Binding SettingProjectInfomation.PathFolderModel, UpdateSourceTrigger=PropertyChanged}"
                VerticalContentAlignment="Center"
                VerticalAlignment="Top"/>
            <Button
                Command="{Binding SelectFolderModelsCmd}"
                Content="..."
                VerticalAlignment="Top"
                Grid.Column="1"/>
        </Grid>
        <DataGrid 
            x:Name="dataModelProjects"
            ItemsSource="{Binding SettingProjectInfomation.ProjectInfomations, UpdateSourceTrigger=PropertyChanged}"
            Grid.Row="1"
            AutoGenerateColumns="False"
            CanUserDeleteRows="False"
            CanUserAddRows="False"
            HeadersVisibility="None"
            ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTemplateColumn 
                    Header="Name"
                    CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox 
                                Text="{Binding ProjectNameInExcel, UpdateSourceTrigger=PropertyChanged, Delay=100}"
                                VerticalContentAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn 
                    Header="Name In Revit"
                    CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox
                                Grid.Column="1"
                                DisplayMemberPath="Name"
                                SelectedItem="{Binding ProjectNameInRevit, UpdateSourceTrigger=PropertyChanged}"
                                ItemsSource="{Binding ProjectNameInRevits}"
                                IsEditable="True"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right"
            Grid.Row="2">
            <StackPanel 
            Orientation="Horizontal">
                <Button
                    Command="{Binding OnAddModelsCmd}"
                    Content="Add"
                    Width="70"/>
                <Button
                    Command="{Binding OnRemoveModelsCmd}"
                    Margin="10,0,0,0"
                    Content="Remove"
                    Width="70"/>
                <Button
                    Command="{Binding OnResetCmd}"
                    Margin="10,0,0,0"
                    Content="Reset"
                    Width="70"/>
                <Button
                    Command="{Binding OnOkCmd}"
                    Margin="10,0,0,0"
                    Content="OK"
                    Width="70"/>
                <Button
                    Command="{Binding OnCancelCmd}"
                    Margin="10,0,0,0"
                    Content="Cancel"
                    Width="70"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
